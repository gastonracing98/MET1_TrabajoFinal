using System;
using System.Collections.Generic;
using System.Text;

namespace MET1_CLASS1_INTERFACES
{//creacion de SUPER CLASE //PASO1
    /*
    interface EstrategiaDeComparacion
    {
        bool sosIgual(IComparable c);
        bool sosMenor(IComparable
            c);
        bool sosMayor(IComparable c);
    }
    //CREACION DE LAS SUB CLASES //PASO 2
    class EstrategiaPorLegajo : EstrategiaDeComparacion // esta clase me ayuda a comparar si es igual o no a otro alumno usando el legajo
    {
        bool sosIgual(IComparable c)
        {
            return alumno.getLegajo() = ((Alumno)c).getLegajo();
        }
        bool sosMenor(IComparable
            c);
        bool sosMayor(IComparable c);
    }
    //creacion de subclases
    class Persona : IComparable
    {
        private string nombre, dni;
    }
    class Alumno : Persona //PASO 3
    {//alumno es nuestro conetexto, ya que exisitia.
        private int legajo, promedio;
        private EstrategiaDeComparacion estrategia;

        //PASO 4
        //CREAION DE ESTRATEGIA POR DEFECTO
        public bool sosIgual(IComparable c)
        {
       
            return legajo = ((Alumno)c).getLegajo();

        }
        public bool sosMenor(IComparable c)
        {

        }
        public bool sosIgual(IComparable c)
        {

        }
        
    }
  */
}
